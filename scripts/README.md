# Scripts

Maintenance and verification scripts live here.

- `verify-e2e.mjs`: checks public pages, admin login, CMS create/update/delete flows, company document holder uploads, project document multi-uploads, uploads, and enquiry handling against a running local site.

Use scripts carefully because some of them may create temporary CMS records while testing and then clean them up.
