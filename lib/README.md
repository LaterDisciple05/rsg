# Lib Folder

Shared application utilities live here.

- `auth.ts`: admin login and session helpers.
- `prisma.ts`: Prisma database client setup.
- `utils.ts`: small reusable utility helpers.

Keep this folder for code that is shared across routes or components. Page-specific logic should stay close to the page that uses it.
